import { rh } from "react-native-full-responsive";
import styled from "styled-components/native";

export const StyledView = styled.View`
  margin-bottom: ${rh(1.5)}px;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
`;
